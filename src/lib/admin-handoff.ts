/** postMessage types — keep in sync with internal-admin `open-fluent-social-profile.ts`. */
export const FLUENT_HANDOFF_REQUEST = "FLUENT_HANDOFF_REQUEST" as const;
export const NEXUS_HANDOFF_TOKENS = "NEXUS_HANDOFF_TOKENS" as const;
